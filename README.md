=== Manifest ===

setup.sh
 - Checks if fish is installed
 - If not, errors
 - If yes, pushes functions, aliases, and config.fish to local

install.sh
 - Updates the system
 - Installs packages (git,fish,tree,btop,fastfetch,curl,wget,build-essential)